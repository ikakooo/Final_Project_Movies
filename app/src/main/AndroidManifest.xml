<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movieapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".AppRoot"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".bottom_navigation.actors.more_actors_activity.MoreActorsActivity" />
        <activity android:name=".bottom_navigation.home.more_movies_activity.MoreMoviesActivity" />
        <activity android:name=".detailed_actors_view.DetailedActorsActivity" />
        <activity android:name=".detailed_movie_view.DetailedMovieActivity" />
        <activity
            android:name=".bottom_navigation.MovieNavigationActivity"
            android:label="@string/title_activity_movie_navigation" />
        <activity android:name=".splash_screen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>